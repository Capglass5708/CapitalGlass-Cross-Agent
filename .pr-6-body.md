## Summary

- **CRITICAL:** Hard-block legacy publication for real harvest identities; enforce universal Git payload retention (manifest + pointer + optional summary only)
- **HIGH:** Prevent skip-ledger-sync from claiming OPERATIONAL; block metadata-only commit loops; redirect graph extraction to L: staging with compact Git pointers
- **MEDIUM:** Decouple basic harvest validation from CG-MASTER-GRAPH availability; portable graph repo resolution; CI `harvest-risk-gates` workflow
- **Block C closeout:** Supabase drift-probe JSON readback fix + platform/harvest/estimating lane receipt (`50dc66d`)
- **Ledger:** Agent 2 DC Synology lane closeout + execution storage gate progress receipts (`6893286` → `7827e75a`)

## Risk mapping

| Risk | Enforcement |
| --- | --- |
| Legacy real harvest path | `harvest-legacy-publication-guard-lib` + CLI guard |
| Full payloads in Git | `harvest-git-retention-lib` universal + staged/CI checks |
| skip-ledger-sync false OPERATIONAL | `harvest-required-layer-policy-lib` |
| Metadata churn | `harvest-metadata-churn-lib` + `check-metadata-churn` |
| Graph payload in Git | `graph-extraction-staging-lib` + pointer validation |
| Absolute path coupling | `graph-repo-resolution-lib` |

## Merge readiness (2026-08-04)

| Gate | Status |
| --- | --- |
| HEAD SHA | `7827e75a9ec85cd3ff972997c59aec68da7aed7e` |
| CI `harvest-authority-gates` | **PASS** ([run 30937050830](https://github.com/Capglass5708/CapitalGlass-Cross-Agent/actions/runs/30937050830)) |
| GitHub merge state | **CLEAN** / **MERGEABLE** |
| L: index publication | **PUBLISH_PASS** @ `7827e75a` |
| Z AI-cache bundle | **CURRENT** @ `7827e75a` |
| D hot routing index | **CURRENT** @ `7827e75a` |
| `agent:index:preflight` | `INDEX_HIT_AI_CACHE`, `freshnessVerdict: CURRENT`, `rawScanRequired: false` |
| AppBuilder drift fix | Landed on `main` as `0776988f` (companion repo) |

## Test plan

- [x] `npm run test:harvest:risk-remediation` (12/12)
- [x] `npm run test:harvest:git-retention` (12/12)
- [x] `npm run test:harvest:layered-verdict` (15/15)
- [x] `npm run test:harvest:authority-dogfood`
- [x] `npm run test:harvest:phase-c`, `test:harvest:content-freshness`, `test:harvest:identity`
- [x] CG-AppBuilder-MCP `test:cross-agent-harvest-projection` (10/10)
- [x] CI `harvest-risk-gates` on this branch
- [ ] Operator merge approval
- [ ] Post-merge: `cross-agent-ledger:ingest --apply` from ext4 checkout @ merged SHA

## Post-merge operator steps

1. Merge this PR to `main` (no unrelated local dirty files required).
2. From WSL ext4 `CapitalGlass-Cross-Agent` @ `main`: `npm run index:publish` if ledger changed on follow-up commits.
3. From `CG-AppBuilder-MCP`: `doppler run -- npm run cross-agent-ledger:ingest -- --apply` then `cross-agent-ledger:drift-probe` → expect **IN_SYNC**.
4. Executor **Wave B** remains separate: blocked on `CG_EXECUTOR_APPROVAL_REF` (run `30935396892` proved runner online; intentional `BLOCKED_OPERATOR_APPROVAL`).

## Non-goals

- No historical harvest deletion
- No Slice 6 republish in this PR
- No executor Wave B mutation (approval ref required)

## Related (not in this PR)

- Office Admin v1.5 drive-mount dedupe: [CapitalGlass-Office-Admin#54](https://github.com/Capglass5708/CapitalGlass-Office-Admin/pull/54) (stacks on mobility branch)
- Executor Wave B: unblock with `CG_EXECUTOR_APPROVAL_REF`, rerun `wsl2-canonical-setup`
