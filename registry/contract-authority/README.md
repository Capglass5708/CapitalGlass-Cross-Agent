# Contract authority — Scraper / Data-Extraction / Master Graph

Pointer-only registry. Authoritative schemas live in owner repositories.

Validate with: `npm run validate:scraper-de-contract-registry` (when wired).
